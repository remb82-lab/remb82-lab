function renderNiches(){const ns=room().geometry.niches||[];$('nicheList').innerHTML=ns.length?ns.map((x,i)=>`<div class="data-row"><div class="row-fields"><label>Ширина<input data-nw="${i}" type="number" step="0.01" value="${x.width||0}"></label><label>Глубина<input data-nd="${i}" type="number" step="0.01" value="${x.depth||0}"></label><label>X<input data-nx="${i}" type="number" step="0.01" value="${x.x||0}"></label><label>Y<input data-ny="${i}" type="number" step="0.01" value="${x.y||0}"></label></div><button class="remove" data-ndel="${i}">×</button></div>`).join(''):'<div class="hint">Ниш нет.</div>';for(const k of ['nw','nd','nx','ny'])qa(`[data-${k}]`).forEach(e=>e.oninput=()=>{const x=ns[+e.dataset[k]],map={nw:'width',nd:'depth',nx:'x',ny:'y'};x[map[k]]=Math.max(0,num(e.value));persist();renderPlan();recalc()});qa('[data-ndel]').forEach(e=>e.onclick=()=>{ns.splice(+e.dataset.ndel,1);persist('Ниша удалена');renderNiches();renderPlan();recalc()})}
const featureNames={light:'Светильник',pipe:'Труба',track:'Трек',cornice:'Карниз'};
function renderFeatures(){const fs=room().features||[];$('featureList').innerHTML=fs.length?fs.map((f,i)=>`<div class="feature-card"><header><b>${esc(f.label||featureNames[f.type]||f.type)}</b><button class="remove" data-fdel="${i}">×</button></header><div class="feature-fields"><label>Кол-во<input data-fqty="${i}" type="number" min="0" step="1" value="${f.qty||1}"></label><label>Длина, м<input data-flen="${i}" type="number" min="0" step="0.01" value="${f.length||0}"></label><label>Угол°<input data-fang="${i}" type="number" step="1" value="${f.angle||0}"></label><label>Цена/ед.<input data-fcost="${i}" type="number" min="0" step="0.01" value="${f.unitCost||0}"></label></div></div>`).join(''):'<div class="hint">Элементов на схеме нет.</div>';for(const [key,field] of [['fqty','qty'],['flen','length'],['fang','angle'],['fcost','unitCost']])qa(`[data-${key}]`).forEach(e=>e.oninput=()=>{fs[+e.dataset[key]][field]=num(e.value);persist();renderPlan();recalc()});qa('[data-fdel]').forEach(e=>e.onclick=()=>{fs.splice(+e.dataset.fdel,1);persist('Элемент удалён');renderFeatures();renderPlan();recalc()})}
function renderMaterials(){const ms=room().materials||[];$('materialList').innerHTML=ms.length?ms.map((m,i)=>{const p=product(m.productId),d=dealerPrice(p);return `<div class="data-row"><div><b>${esc(p?.name||'Позиция')}</b><small>${d?`${fmt(d.amount)} ${d.currency}/${esc(d.unit||p?.default_unit||'')}`:'нет цены'}</small></div><div class="row-fields"><label>Кол-во<input data-mqty="${i}" type="number" min="0" step="0.01" value="${m.qty}"></label></div><button class="remove" data-mdel="${i}">×</button></div>`}).join(''):'<div class="hint">Дополнительных материалов нет.</div>';qa('[data-mqty]').forEach(e=>e.oninput=()=>{ms[+e.dataset.mqty].qty=Math.max(0,num(e.value));persist();recalc()});qa('[data-mdel]').forEach(e=>e.onclick=()=>{ms.splice(+e.dataset.mdel,1);persist('Материал удалён');renderMaterials();recalc()})}
function bounds(points){if(!points.length)return{x:0,y:0,w:6,h:4};const xs=points.map(p=>p.x),ys=points.map(p=>p.y),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys);return{x:minX-.7,y:minY-.7,w:Math.max(3,maxX-minX+1.4),h:Math.max(3,maxY-minY+1.4)}}
function renderPlan(){const r=room(),g=geometrySummary(r),pts=g.points,b=bounds(pts),svg=$('planSvg'),W=1000,H=650,s=Math.min((W-100)/b.w,(H-100)/b.h),map=p=>({x:50+(p.x-b.x)*s,y:50+(p.y-b.y)*s});let h='<rect width="1000" height="650" fill="#071114"/>';for(let x=0;x<=1000;x+=25)h+=`<line x1="${x}" y1="0" x2="${x}" y2="650" stroke="${x%100===0?'#1f353a':'#122126'}"/>`;for(let y=0;y<=650;y+=25)h+=`<line x1="0" y1="${y}" x2="1000" y2="${y}" stroke="${y%100===0?'#1f353a':'#122126'}"/>`;if(pts.length){const pp=pts.map(map);h+=`<polygon points="${pp.map(p=>`${p.x},${p.y}`).join(' ')}" fill="rgba(101,225,207,.08)" stroke="#65e1cf" stroke-width="4"/>`;pp.forEach((p,i)=>{const q=pp[(i+1)%pp.length],mid={x:(p.x+q.x)/2,y:(p.y+q.y)/2},len=Math.hypot(pts[(i+1)%pts.length].x-pts[i].x,pts[(i+1)%pts.length].y-pts[i].y);h+=`<text x="${mid.x}" y="${mid.y-8}" text-anchor="middle" fill="#d9e7e6" font-size="17">${fmt(len)} м</text><circle data-vertex="${i}" cx="${p.x}" cy="${p.y}" r="10" fill="#b8ff6d"/><text x="${p.x+13}" y="${p.y-13}" fill="#fff" font-size="18">${String.fromCharCode(65+i)} ${g.angles[i]?fmt(g.angles[i],0)+'°':''}</text>`});(r.geometry.diagonalMeasurements||[]).forEach(d=>{if(!pp[d.a]||!pp[d.b])return;h+=`<line x1="${pp[d.a].x}" y1="${pp[d.a].y}" x2="${pp[d.b].x}" y2="${pp[d.b].y}" stroke="#889ca0" stroke-width="2" stroke-dasharray="8 8"/>`})}
(r.geometry.niches||[]).forEach(n=>{const p=map(n),w=num(n.width)*s,hh=num(n.depth)*s;h+=`<rect x="${p.x}" y="${p.y}" width="${w}" height="${hh}" fill="rgba(255,190,80,.16)" stroke="#ffbd58" stroke-width="2"/><text x="${p.x+4}" y="${p.y+18}" fill="#ffd99b" font-size="15">ниша</text>`});(r.features||[]).forEach(f=>{const p=map(f),sym={light:'●',pipe:'◎',track:'━',cornice:'═'}[f.type]||'◆';h+=`<text x="${p.x}" y="${p.y}" text-anchor="middle" dominant-baseline="middle" fill="#ffda76" font-size="${f.type==='track'||f.type==='cornice'?30:24}" transform="rotate(${num(f.angle)} ${p.x} ${p.y})">${sym}</text>`});svg.innerHTML=h}
function world(e){const svg=$('planSvg'),r=svg.getBoundingClientRect(),g=geometrySummary(room()),b=bounds(g.points),s=Math.min(900/b.w,550/b.h),px=(e.clientX-r.left)/r.width*1000,py=(e.clientY-r.top)/r.height*650,snap=v=>Math.max(0,Math.round(v*4)/4);return{x:snap((px-50)/s+b.x),y:snap((py-50)/s+b.y)}}
async function renderPhotos(){for(const u of photoUrls)URL.revokeObjectURL(u);photoUrls=[];const r=room();$('photoGrid').innerHTML='';for(const p of r.photos||[]){try{const blob=await getPhotoBlob(p.id);if(!blob)continue;const u=URL.createObjectURL(blob);photoUrls.push(u);const d=document.createElement('div');d.className='photo';d.innerHTML=`<img src="${u}" alt="Фото"><button data-photo="${p.id}">×</button>`;$('photoGrid').append(d)}catch{}}qa('[data-photo]',$('photoGrid')).forEach(b=>b.onclick=async()=>{await deletePhoto(b.dataset.photo);r.photos=r.photos.filter(x=>x.id!==b.dataset.photo);await persist('Фото удалено');renderPhotos()})}
function recalc(){const r=room();if(!r)return;const g=geometrySummary(r);$('metricArea').textContent=`${fmt(g.area)} м²`;$('metricPerimeter').textContent=`${fmt(g.perimeter)} м`;$('metricCorners').textContent=String(g.points.length);$('metricNiches').textContent=String(r.geometry.niches?.length||0);const c=calc();const p=product(r.pricing.profileProductId),d=dealerPrice(p),rr=referencePrice(p);$('profileHint').textContent=p&&d?`Поставщик ${fmt(d.amount)} ${d.currency}/${d.unit||p.default_unit}${rr?` · МРЦ ${fmt(rr.amount,0)} ${rr.currency}`:''}`:'Профиль не выбран';return c}
function renderRoom(){const r=room();if(!r)return;$('roomHeading').textContent=r.name;$('rectLength').value=r.geometry.length??'';$('rectWidth').value=r.geometry.width??'';$('wastePct').value=r.pricing.wastePct??8;$('membraneCost').value=r.pricing.membraneCostPerM2??8;$('laborCost').value=r.pricing.laborCostPerM2??12;$('roomNotes').value=r.notes||'';$('profileSelect').innerHTML=profileOptions();$('profileSelect').value=r.pricing.profileProductId??'';setMode(r.geometry.mode||'rect',false);renderVertices();renderDiagonals();renderNiches();renderFeatures();renderMaterials();renderPlan();renderPhotos();recalc()}
function renderAll(){syncFields();$('dbMeta').textContent=`БД: ${DB.meta?.productCount||DB.products?.length||0} товаров · ${DB.meta?.priceCount||'—'} цен`;renderStatus();renderTabs();renderRoom();renderProjectList()}
